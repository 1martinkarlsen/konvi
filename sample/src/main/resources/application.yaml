konvi:
  port: ${PORT:8080}
  database:
    url: ${DB_URL:jdbc:h2:mem:test;DB_CLOSE_DELAY=-1}
    driver: ${DB_DRIVER:org.h2.Driver}
    username: ${DB_USERNAME:sa}
    password: ${DB_PASSWORD:}
  cors:
    allowedOrigins:
      - "http://localhost:3000"
  auth:
    jwt:
      secret: ${JWT_SECRET:dev-secret-change-me}
      issuer: konvi-sample
      audience: konvi-sample
      realm: Konvi
      expiresInSeconds: 3600
