CREATE TABLE IF NOT EXISTS USERS (ID BIGINT AUTO_INCREMENT PRIMARY KEY, "name" VARCHAR(80) NOT NULL, EMAIL VARCHAR(80) NOT NULL, PASSWORD VARCHAR(80) NOT NULL, CREATED_AT DATETIME(9) NOT NULL, EMAIL_VERIFIED BOOLEAN NOT NULL);
